import React from 'react'
import Auth from '../components/signup/Auth'

const SignUp = (props) => {
    return (
        <div>
          <Auth {...props}/>  
        </div>
    )
}

export default SignUp
